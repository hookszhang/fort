before_script:
    - java -version && mvn -v && npm -v && gulp -v
job1:
    script:
        - ./mvnw -Pprod package
        - \cp target/*.war ~/servers/fort-server-9800/webapps/ROOT.war
    only:
        - master
    tags:
        - fort
